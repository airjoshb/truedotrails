<div class="mt-4 max-w-md mx-auto">
  <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: "card" }) do |f| %>
    <h1 class="mt-2 mb-6 text-xl">Resend unlock instructions</h1>

    <%= render "shared/flash", flash: resource.errors %>

    <div class="field">
      <%= f.label :email %>
      <%= f.email_field :email, autocomplete: "email", class: "input" %>
    </div>

    <div class="buttons">
      <%= f.submit "Resend", class: "btn btn--primary" %>
    </div>
  <% end %>

  <%= render "users/shared/links" %>
</div>

